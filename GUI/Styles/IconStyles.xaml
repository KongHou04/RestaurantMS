<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp">


    <Style x:Key="IconStyle1" TargetType="fa:IconImage">
        <Setter Property="Width" Value="15"/>
        <Setter Property="Foreground" Value="#292929"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Panel.ZIndex" Value="1"/>
        <Setter Property="Margin" Value="5 0"/>
    </Style>

    <Style x:Key="BindingColorIconStyle1" TargetType="fa:IconImage">
        <Setter Property="Foreground" Value="{Binding Path=Foreground, RelativeSource= {RelativeSource AncestorType=RadioButton}}"/>
    </Style>

    <Style x:Key="TitleIconStyle1" TargetType="fa:IconImage">
        <Setter Property="Height" Value="24"/>
        <Setter Property="Margin" Value="0 1 0 0"/>
        <Setter Property="Foreground" Value="{StaticResource color2}"/>
    </Style>

    <Style x:Key="backIconStyle1" TargetType="fa:IconImage">
        <Setter Property="Icon" Value="ArrowLeft"/>
        <Setter Property="Height" Value="13"/>
        <Setter Property="Height" Value="13"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{StaticResource color8}"/>
    </Style>


</ResourceDictionary>